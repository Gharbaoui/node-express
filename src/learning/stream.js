const {createReadStream} = require('fs');

/*
const bigFileStream = createReadStream('./conent/big.txt');

bigFileStream.on('data', (res) => {
    console.log(res);
});

bigFileStream.on('error', (err) => {
    console.log(`my error: ${err}`);
}); */