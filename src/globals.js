// globals are some variables or function that are available to all files no matter how much nested are
/*
    for example:
        __dirname is variable that has info about current directory
        require is function to load other modules
*/

console.log(__dirname);